# Jack's Neovim Configuration

This is my configuration for `nvim`. I have opted for a minimal text-editing environment tailored for programming.

# Setup

The following sections detail how to setup this project.

```shell
git clone https://github.com/devjacjef/nvim ~/.config/nvim
make install
```
